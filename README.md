# Router
 Docker file for routing the routes

Uses NGINX to route incoming routes to the proper docker container on the network.

There is a whole network of docker containers running apps.

This router does the routing.